---
import type { Project } from "../data";

interface Props {
  project: Project;
}

const { project } = Astro.props;
---

<div class="group bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden hover:shadow-lg hover:shadow-accent/20 transition-all duration-300">
  <div class="aspect-video overflow-hidden bg-gray-100">
    <img
      src={project.image}
      alt={project.title}
      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
      loading="lazy"
    />
  </div>
  <div class="p-6">
    <h3 class="text-lg font-semibold mb-2">{project.title}</h3>
    <p class="text-gray-600 text-sm mb-1">{project.description}</p>
    <p class="text-gray-500 text-sm mb-4">{project.detail}</p>
    <div class="flex gap-3">
      {project.url && (
        <a
          href={project.url}
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-accent hover:text-accent-hover font-medium transition-colors"
        >
          Visit Site &rarr;
        </a>
      )}
      {project.repo && (
        <a
          href={project.repo}
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-gray-600 hover:text-gray-900 font-medium transition-colors"
        >
          Source Code &rarr;
        </a>
      )}
    </div>
  </div>
</div>
